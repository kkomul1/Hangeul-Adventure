# HangeulAdventure

한글 자모를 이동/회전/합성/분해해 글자와 단어를 만드는 그리드 퍼즐 어드벤처. Steam 출시 목표. Unity 6 (6000.5.1f1), URP, 2D.

## 필독 문서
- **`Docs/퍼즐규칙명세 (밀기·합성·분해·회전·수집 규칙의 단일 기준 SSOT).md`** — 퍼즐 규칙의 단일 기준(SSOT). 규칙 엔진·솔버·스테이지 작업 전에 반드시 읽을 것. 초안과 다르면 명세가 우선.
- **`Docs/게임기획초안 (콘셉트·스토리·시장 전략, 규칙 장은 명세로 대체됨).md`** — 콘셉트·스토리·난이도 구조·시장 전략 참고 문서. 규칙 부분(2~7장)은 명세로 대체됨.

## 개발 방향
- MVP 최우선: 2x2~4x4 보드(비정형 포함), 전체 자모 지원 엔진, 밀기(이동/합성/분해 통합), 이동 수+별+루비 별, 수집식 목표, 스테이지 30개 이상. 검증 질문은 "ㄱ과 ㅏ를 움직여 가를 만드는 게 재미있는가".
- 퍼즐 규칙 엔진(합성/분해 판정, 보드 상태)은 MonoBehaviour와 분리된 순수 C#으로 작성 — EditMode 테스트와 솔버가 재사용해야 함.
- 스테이지는 코드가 아닌 데이터(JSON/ScriptableObject)로 정의.

## 서브에이전트 (.claude/agents/)
- `game-designer` — 기획, 밸런싱, 진행 구조
- `puzzle-level-designer` — 스테이지 제작 + 솔버 기반 풀이 가능성 검증
- `asset-designer` — 아트 디렉션, 에셋 스펙 (옛한글 폰트 이슈 담당)
- `unity-code-reviewer` — Unity C# 리뷰 (코드 작성 후 사용)
- `qa-playtester` — 규칙 엔진 테스트, 엣지 케이스

## Unity MCP
- `.mcp.json`에 unityMCP 등록됨 (MCP for Unity v10). **Unity 에디터가 이 프로젝트를 열고 있어야 도구가 작동함** — 연결 실패 시 에디터 실행 여부부터 확인.
- 에디터 쪽 창: `Window → MCP for Unity → Toggle MCP Window`

## 지침
- 답변은 한국어로 작성한다.

## 미리보기 게이트 (시각 변경 승인 절차)
- **눈에 보이는 변화**(UI, 연출, 화면 흐름, 비주얼, 맵 외형)를 만드는 작업은 코드를 수정하기 **전에** "현재 vs 변경 후" 미리보기 목업을 보여주고, 사용자 승인 후 구현한다.
- **1순위 경로 — 승인 보드**: approval-board MCP의 `propose_change(title, html, ...)`로 목업을 게시하고 `await_decision(id)`을 결정될 때까지 반복 호출해 승인/반려/코멘트를 받는다 (보드: http://127.0.0.1:7777, 반려 시 코멘트를 반영해 재제안하거나 중단). 보드가 응답하지 않으면(서버 꺼짐) visualize 위젯 + 채팅 질문으로 폴백.
- 보드 사용 요점: A/B/C 복수 안은 `variants`로 (안별 선택 버튼 생성, decision=선택된 안의 label). `await_decision`의 timeout_seconds는 55 이하로 반복 호출. 결과에 `attachments`(사용자 첨부 이미지 로컬 경로)가 있으면 반드시 Read로 열어 반영. 목업은 카드 폭 전체를 쓰고, 세로쓰기·vh 등 뷰포트 종속 레이아웃엔 px 명시 높이를 줄 것 (2,400px 초과분은 카드 내 스크롤됨).
- 미리보기는 HTML 근사 목업이므로 실제 Unity 렌더링과 다를 수 있음을 한 줄로 명시한다. 구현 후에는 실제 스크린샷으로 결과를 재확인시킨다.
- 화면 변화가 없는 작업(순수 로직, 데이터, 버그 수정, 리팩토링)은 게이트 없이 진행한다.
- **배치 작업**(사용자가 신호를 줘 여러 작업을 한꺼번에 실행할 때)은 매 항목마다 멈추지 않는다 — 시작 전에 시각 변경 항목들의 미리보기를 한꺼번에 모아 보여주고 일괄 승인을 받은 뒤 중단 없이 실행한다.
- 사용자가 "바로 구현해"라고 명시한 작업은 게이트를 건너뛴다.
