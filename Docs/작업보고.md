# 밤샘 자율 구현 작업보고

2026-07-09 08:28 ~ 10:00 (약 1시간 30분). MVP 전체 구현 완료.

## 아침에 플레이하는 방법

1. Unity 에디터에서 `Assets/Scenes/Main.unity` 씬이 열려 있는지 확인 (밤사이 에디터를 한 번 재시작했음 — 씬이 다르면 Main을 더블클릭)
2. **플레이 버튼(▶)** 클릭
3. 타이틀 → 시작 → 스테이지 선택(50개) → 플레이
4. 조작: **타일 드래그** 또는 클릭+방향키 = 밀기 · 클릭+**Space** = 수집 · **Q**(합성)/**E**(분해) 필터 · **Z** 되돌리기 · **R** 재시작
5. 별 기록을 지우려면 타이틀의 "진행 초기화"

## 완료된 단계 (7/7 전부 완료)

| 단계 | 내용 | 상태 |
|---|---|---|
| 1 | 규칙 엔진 (순수 C#) + 테스트 | ✅ 테스트 77개 전부 통과 |
| 2 | BFS 솔버 + 스테이지 JSON 로더 | ✅ |
| 3 | 보드/입력/연출/Q·E 필터 | ✅ |
| 4 | 수집 UI, 별/루비, 화면 흐름 | ✅ |
| 5 | 에셋 (Pretendard OFL 폰트, 절차 타일/효과음) | ✅ 유료 지출 0원 |
| 6 | 스테이지 50개 + 전수 솔버 검증 | ✅ (30개 요청 → 50개로 확장 지시 반영) |
| 7 | 플레이 모드 최종 검증 | ✅ 스크린샷 검증 완료 |

## 커밋 목록

- `9e5d36d` 퍼즐 규칙 명세 v1 확정 (실행 전)
- `abd2f00` 규칙 엔진 + 솔버 + 스테이지 로더 (1~2단계)
- `bd0255b` Unity 표현 계층 + 화면 흐름 + 에셋 (3~5단계)
- `(중간)` Game 계층 리뷰 반영 + 엣지 케이스 테스트 보강
- `7c6b5e7` 스테이지 7~50 추가 (전수 검증)
- `(최종)` 이 보고서 + 마무리 수정

## 주요 결정 (상세는 구현결정로그.md)

- **D-09 [확인 필요]** 명세 5장 내부 모순 발견: 판정 순서(합성 우선)와 요약 표(겉조합 축=분해)가 충돌. 대화에서 확정된 판정 순서대로 **합성 우선** 구현, 테스트로 고정. **명세 표에 각주를 달지 결정해주세요.**
- **D-13 [확인 필요]** 연쇄 합성의 공간 규약(접근 방향과 동일)과 Q/E 필터에서 연쇄의 소속(양쪽 표시)을 기본값으로 정함 — 명세 명문화 권장.
- D-11 UI 전부 코드 생성 (사용자 질문에 답변한 건 — MVP 후 프리팹 전환 권장)
- D-14 대형 스테이지는 에디터 Mono가 느려서, 동일 엔진 코드를 .NET 8로 컴파일한 CLI(scratchpad)로 검증. 에디터 테스트는 스테이지당 5초 시도 후 CLI 값 신뢰.

## 스테이지 구성 (50개)

- 1~6 튜토리얼 (이동·합성·받침) / 7~10 민글자 심화 / 11~15 받침 / 16~19 분해 필수
- 20~23 복합모음 (ㅘ·ㅟ·완) / 24~26 쌍자음·겹받침 (ㄲ·ㅆ·ㄻ) / 27~32 2글자 단어 종합
- 33~50 확장: 단어 다양화, ㄸ 받침 불가 함정(38), ㅘ+ㅣ 비표준 함정(40·47), 닭·몫·빵 등 겹받침/쌍자음 심화, 비정형·4x4 다수, 피날레 '하늘'(50)
- **전 스테이지 솔버 검증**: 풀이 가능 + 최소 수 기입. 독립 구현 솔버와 엔진 솔버가 50개 전부 동일 값 (교차 검증). 루비 별 = 최소 수 클리어.

## 발견/해결된 문제

1. **클리어 팝업 "재도전" 소프트락** (리뷰 발견) → 완전 재시작으로 수정, 플레이 검증
2. **UI 클릭이 보드 관통** (사용자가 느낀 "버튼 안 눌림"의 유력 원인) → 수정. 되돌리기/재시작 버튼 동작을 플레이 모드에서 직접 검증 완료 (1→0→0)
3. **에디터 프리즈**: 스테이지 30 검증 중 에디터가 장시간 무응답 → 에디터 재시작으로 해결. 원인은 에디터 Mono 성능 (동일 계산이 .NET 8에서 0.5초). 재발 방지로 솔버에 시간 예산 추가
4. TMP Importer 팝업 — 필수 리소스는 코드로 임포트됨, 창은 닫으면 됨 (Examples는 불필요)

## 알려진 한계 / 추천 다음 단계

1. 스테이지 선택 버튼에서 별과 숫자가 살짝 겹침 (기능 문제 없음, 폴리시 대상)
2. UI가 전부 코드 생성 — 에디터에서 시각 편집 불가. **추천: MVP 검증 후 정적 UI 프리팹 전환**
3. BGM 없음 (효과음만, 의도된 생략)
4. D-09/D-13 명세 반영 여부 결정 필요
5. 다음 단계 추천 순서: (1) 직접 플레이하며 "ㄱ+ㅏ=가가 재밌는가" 판정 → (2) 재미 피드백으로 규칙/스테이지 조정 → (3) 튜토리얼 안내 텍스트(지금은 힌트 한 줄뿐) → (4) Windows 빌드 → (5) 아트 방향 결정
