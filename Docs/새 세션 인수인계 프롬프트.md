# 새 세션 첫 프롬프트

아래를 복사해 새 세션 첫 메시지로 붙여넣으세요.
(메모리의 `m7-handover`가 자동 로드되므로 이 프롬프트는 진입 지시입니다.)

---

```
한글 어드벤처(C:\Users\minjae\UnityProjects\HangeulAdventure) 작업을 이어받는다.

1. 메모리의 m7-handover가 단일 진입점이다 — 정독하고 시작하라. m6를 대체한다.
   시작 전에 art-verification-lesson(이전 세션 최대 실수)과 m7 5장 함정 목록을
   반드시 정독할 것. 아트 관련 작업이 있으면 특히.

2. 시작 절차:
   ① 도구 연결 확인 — unityMCP(Unity 에디터가 이 프로젝트를 열고 있어야 함),
      approval-board(127.0.0.1:7777), pixellab(생성 잔액 확인)
   ② git status로 미커밋 변경 확인 → 있으면 컴파일·EditMode 테스트(97개 기준)·
      실플레이 스크린샷 검증 후 커밋. 없으면 트리 클린 상태로 시작.
   ③ EditMode 테스트 97개 그린 확인.
   ④ 현재 상태와 대기 결정을 요약해 보고하고 대기.

3. ★보고 방식: 결과 보고와 판단이 필요한 결정은 채팅이 아니라 승인 보드로 올린다
   (report_result → /reports 탭, 대기 결정은 propose_change의 variants).
   채팅에는 "무엇을 했다" 짧은 확인만. 사용자 피드백은 fetch_feedback으로 회수.

4. ★아트 채택 규칙: 에셋을 개별로만 보지 말고 (1) 나란히 비교 (2) PIL 배치 시뮬레이션
   (3) 가장자리 픽셀 실측 — 3개를 통과시킨 뒤 채택하라. 서브에이전트의 "안정적/일관적"
   형용사를 믿지 말고 대조 시트를 직접 봐라. 시각 변경은 반드시 플레이 모드 실제 스크린샷으로 확인.

5. 작업 규칙: 답변 한국어, 시간 추정 금지, 커밋 메시지 한국어, 막히면 적극 복구
   (에디터 재시작 포함). 대형 배치는 파일 단위로 서브에이전트에 배정하고 컴파일은 메인이 일괄.
```

---

## 시작 전 체크리스트
- Unity 에디터가 이 프로젝트를 열고 있을 것
- 승인 보드 서버(127.0.0.1:7777) 켜둘 것

## 다음 세션의 핵심 후속 (m7-handover에 상세)
1. **신규 프롭 24종을 맵에 배치 배선** — 에셋은 임포트됐으나 맵 decorations에 안 깔림
2. **오프닝 4컷 컷신** — 이번 세션에서 배선 착수(완료 여부는 git status로 확인)
3. **사이드뷰 맵 정식 전환** — 사용자가 "아직 전환 말고 둘 다 유지" 결정. map_101 출구는 102로 고침(순회 가능). 전환 시점은 사용자 결정 대기
4. **커스텀 맵 역치 UI** — danger 판정식(A·log₁₀(m)+Σlog₁₀(회전순환) < 6.30) 에디터 배선, 검증 비동기화(60분 동기는 앱 정지+OOM이라 불가)
5. **작심삼일 20수 재도전** — 솔버 빨라졌으니 바위로 통로 조여 재시도 가능(현재 16수)
